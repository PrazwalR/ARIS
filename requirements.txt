# Convenience install for development: the package plus its dev tooling.
#   pip install -r requirements.txt
#
# Runtime dependencies live in pyproject.toml and are installed by `-e .`.
# The federated-learning (M1) and API (M4) stacks are optional extras and are
# deliberately NOT installed here -- nothing in the codebase imports them yet,
# and torch alone is ~2.5GB.
#   pip install -e ".[ml]"   # when M1 starts
#   pip install -e ".[api]"  # when M4 starts

# Convenience install for development: the package plus its dev tooling.
#   pip install -r requirements.txt
#
# Runtime dependencies live in pyproject.toml and are installed by `-e .`.
# The federated-learning (M1) and API (M4) stacks are optional extras and are
# deliberately NOT installed here -- nothing in the codebase imports them yet,
# and torch alone is ~2.5GB.
#   pip install -e ".[ml]"   # when M1 starts
#   pip install -e ".[api]"  # when M4 starts

-e .[dev]
